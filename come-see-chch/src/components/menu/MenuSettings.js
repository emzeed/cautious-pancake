import React, { Component } from 'react'

export default class MenuSettings extends Component {
    render(props) {
        return (
            <button className={this.props.classname}>Settings</button>
        )
    }
}
